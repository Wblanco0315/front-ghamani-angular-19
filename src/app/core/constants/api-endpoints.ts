export class ApiEndpoints {
  static readonly BASE_URL = 'http://localhost:8080/api/';
  static readonly AUTH = `${ApiEndpoints.BASE_URL}auth/`;
  static readonly PRODUCTS = `${ApiEndpoints.BASE_URL}productos`;
}
